[project]
name = "DDOS-detection"
version = "0.1.0"
description = "DDOS detection neural network"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.3",
    "pandas>=3.0.1",
    "torch>=2.10.0",
]
